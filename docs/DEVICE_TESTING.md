# Device verification — entirely unverified

Use an Apple-Intelligence-capable physical iPhone with the supported iOS 27 release, plus unavailable/disabled configurations. Record device, OS/build, application commit and observed result. No boxes below were completed during this task.

- [ ] Capability environment correctly reflects Image Playground availability.
- [ ] Sheet opens and is locked to emoji generation.
- [ ] All three named concepts and intended roles are passed.
- [ ] Cancel creates no node and never auto-advances.
- [ ] Accept returns a real NSAdaptiveImageGlyph.
- [ ] Original imageContent, identifier and contentDescription persist.
- [ ] Reconstructed glyph displays after termination, relaunch and device restart.
- [ ] CoreText rendering preserves bounds, transparency and usable source imagery.
- [ ] Semantic, visual and hybrid inheritance work from accepted parents.
- [ ] Auto-advance waits for actual dismissal, persists once, and respects stop/limits.
- [ ] Background/foreground, interrupted callbacks, retries and stale sessions are safe.
- [ ] Foundation Models availability reasons are handled and invalid/slow results fall back.
- [ ] Historical branching preserves descendants and changes only active parent state.
- [ ] Current-run and all-history repeat policies work across relaunch and run changes.
- [ ] Ratings update a locally retained, resettable preference model after the data gate.
- [ ] Metadata export includes provenance and no binary glyph content.
- [ ] Copy Description is correctly labeled; any future native copy interoperates with compatible text surfaces.
- [ ] VoiceOver, largest accessibility text, Dark Mode, contrast, Reduce Motion and tap targets are checked.
- [ ] Unsupported devices explain the capability limitation without losing existing lineage.
- [ ] Network/runtime privacy audit confirms final implementation and dependency declarations.

Storage tests using arbitrary fixture bytes do not validate adaptive-glyph encoding or rendering. macOS source/test success does not count as a simulator or device result.
