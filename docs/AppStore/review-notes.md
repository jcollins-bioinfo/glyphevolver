# Review notes — future release checklist, not submission-ready

No login is required. The intended native creation flow presents Apple's system-controlled Image Playground sheet, locked to emoji mode. Every result remains user-accepted. Compatible Apple Intelligence/Image Playground availability on a supported iOS 27 device is required for true adaptive glyph generation.

The current development milestone does **not** implement that host and should not be submitted for review. On-device Foundation Models enrichment/critique is also not implemented. A deterministic local search and fallback phenotype analyzer exist; only these implemented capabilities can currently be claimed.

Before submission, add reproducible reviewer steps for capability handling, first accepted glyph, cancellation, lineage, semantic/visual/hybrid inheritance, auto-advance and privacy controls, using a validated release build.
